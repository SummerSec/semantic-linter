<!-- STL:RULES:BEGIN -->
## 语义约束规则（核心原&#21017;）

> 本区块由 `npm run build-rules` 生成，请勿手动编辑。

写 skill / agent / command / prompt 等指令文件前，先读同目录 `semantic-rules.md`（27 对宽→窄词 + 四维判定）；据其收窄用词。必须保留宽词时，套用其中的边界锚定策略。
<!-- STL:RULES:END -->
