<!-- STL:RULES:BEGIN -->
## 语义约束规则（按需加载）

> 本区块由 `npm run build-rules` 生成，请勿手动编辑。

同目录下的 `semantic-rules.md` 收录了语义陷阱词对照表（宽边界 → 窄边界，共 27 对）与四维收窄判定标准。

**何时打开它**：编写或修改 skill / agent / command / prompt 等指令类文件前，先读 `semantic-rules.md`，据其把宽边界用词替换为更窄的用词；必须保留宽边界词时，套用其中的边界锚定策略。
<!-- STL:RULES:END -->
